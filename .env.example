# Optional override for the Clarity project id committed in lib/analytics.ts.
# Set it to point a preview deployment at a different Clarity project.
# Read at build time, so a change needs a rebuild.
NEXT_PUBLIC_CLARITY_PROJECT_ID=
